Basic Qt GUI interface to the bc calculator program.

The bc utility must be installed for this to function properly.

BUILD
make

RUN
./qtbc

Also note that qtbc-gui is a stand-alone application. Try running it. What happens when you use the keyboard to enter lines of standard input?
